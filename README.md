# Personal Website

This repository hosts a static personal website for GitHub Pages.

## Structure

- `index.html`: homepage entrypoint.
- `assets/styles/home.css`: homepage styles.
- `CNAME`: custom domain configuration for GitHub Pages.

## Notes

- Keep `index.html` and `CNAME` at repository root for GitHub Pages compatibility.
- Replace placeholder blocks marked "Insert Content" with final content when ready.
